#!/usr/bin/env bash
# Ansible managed
set -uo pipefail

CHECK_URL={{ app_url | quote }}
PING_URL={{ uptime_ping_url | quote }}

if curl -fsS -m 15 -o /dev/null "$CHECK_URL"; then
  curl -fsS -m 15 --retry 3 -o /dev/null "$PING_URL"
fi
